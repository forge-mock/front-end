import { getLocalStorage, setLocalStorage, removeLocalStorage } from "./local-storage";
import { getCookie, setCookie } from "./cookies";

export { getCookie, setCookie, getLocalStorage, setLocalStorage, removeLocalStorage };
